#include "main.h"

/**
 * int _islower - check if letter isa lowercase
 *
 * Return 1 if is true and 0 if else
*/

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else

		return (0);
}
