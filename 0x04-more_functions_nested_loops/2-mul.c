#include "main.h"

/**
 * mul - multiplies two integers
 * @a: are the given values
 * @b: are the given values
 * Return: result
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
