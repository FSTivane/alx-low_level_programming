#include "main.h"
i/**
 * _puts - prints a string
 *
 */

void _puts(char *str)
#include<stdio.h>
/**
 * main - Entry point
 * _puts - prints a string
 * Return: Always 0 (Success)
 */

int main(void)
{
	/**
	 * _puts - prints a string
	 */

	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

